stores:
  00000000-0000-0000-0000-000000000000:
    endpoint_url: http://localhost:9000
    bucket: rdpms
    # The tool scans below this prefix. Registered object keys are sent relative to this prefix.
    # Keep this aligned with the RDPMS S3 datastore KeyPrefix.
    prefix: data/raw-rosbags/
    region:
    use_path_style: true
    access_key: minioadmin
    secret_key: minioadmin
    # session_token:
